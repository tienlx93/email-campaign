# API proxy target — set to where the API server is listening
# Local dev: http://localhost:3000
# Docker Compose: set via docker-compose.yml environment block (http://api:3000)
VITE_API_URL=http://localhost:3000
